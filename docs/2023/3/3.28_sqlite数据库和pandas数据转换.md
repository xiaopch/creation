## sqlite数据库和pandas数据转换

####  MySQL数据导入sqlite3

```
### MySQL数据导入sqlite3
import pandas as pd
import sqlite3
#connect to the database
conn = sqlite3.connect('mysql_sync.db')

# 导入pymysql模块
import pymysql
 
# 连接mysql database
mysql_conn = pymysql.connect(
    host= 'rm-123.mysql.rds.aliyuncs.com',
    user= 'root',password= '',
    database= 'ziys',
    charset='utf8')
table_name=['user_information','user','video','user_behavior','long_video']

# df = df.loc[:, ~df.columns.str.contains('^Unnamed')]
for tbn in table_name:
    df=pd.read_sql_query("select * from {}".format(tbn), mysql_conn)
    df.to_sql(tbn,conn,schema=None,if_exists='replace',index=True,index_label=None, chunksize=None, dtype=None)

# df_user_behavior.to_sql('user_behavior',conn,schema=None,if_exists='replace',index=True,index_label=None, chunksize=None, dtype=None)
#close out the connection
mysql_conn.close()
conn.close()
```



### 列出一些重点方法

```
pandas.read_sql(sql, conn, index_col=None, coerce_float=True, params=None, parse_dates=None, columns=None, chunksize=None)

DataFrame.to_sql(name, conn, flavor=None, schema=None, if_exists='fail', index=True, index_label=None, chunksize=None, dtype=None)

if_exists='replace'，如果存在指定表(name)，则替换。
if_exists='append'，如果存在指定表(name)，则追加。
if_exists='fail'，如果存在指定表(name)，则写入失败。
chunksize，例如chunksize = 1000，每次处理1000条，避免数据量过大。

conn = sqlite3.connect("test.db")：建立与数据库的连接，如果数据库不存在则建立数据库
conn = sqlite3.connect(":memory:")：将数据库建在内存中
```



### 读取csv文件

In [ ]:

```
import pandas as pd
df_all=None
try:
    df_all = pd.read_csv('./simplifyweibo_4_moods/simplifyweibo_4_moods.csv')
except Exception as err:
    print(str(err))

# df_all['label'].replace(0, '喜悦', inplace=True)
df_all['emotion'] = df_all['label'].map({0:"喜悦", 1:"愤怒", 2:"厌恶", 3:"低落"})
df_all.drop(labels='label',axis=1,inplace=True)
df_all.rename(columns = {'review':'text','emotion':'label'}, inplace = True)
df_all.to_csv('simplifyweibo_4_moods_ds.csv')
df_all.sample(10)
```

In [ ]:

```
import pandas as pd
df_all = pd.read_csv('./OCEMOTION.csv')
df_all
```

In [ ]:

```
# # df_all['label'].replace(0, '喜悦', inplace=True)
df_all['label'] = df_all['emotion'].map({'happiness':'幸福','sadness':'悲伤','disgust':'厌恶','anger':'愤怒','like':'喜欢','surprise':'惊喜','fear':'恐惧'})
df_all.drop(labels='emotion',axis=1,inplace=True)
# df_all.rename(columns = {'review':'text','emotion':'label'}, inplace = True)
df_all.to_csv('OCEMOTION_ds.csv')
df_all.sample(100)
```

In [ ]:

```
import pandas as pd
df_all=None
try:
    df_all = pd.read_csv('./data/usual_eval_labeled.csv')
except Exception as err:
    print(str(err))

# # # df_all['label'].replace(0, '喜悦', inplace=True)
df_all['label'] = df_all['情绪标签'].map({'happy':'幸福','sad':'悲伤','angry':'愤怒','neutral':'中性','surprise':'惊喜','fear':'恐惧'})
df_all.drop(labels='情绪标签',axis=1,inplace=True)
df_all.rename(columns = {'文本':'text'}, inplace = True)
df_all.to_csv('usual_eval_labeled.csv')
df_all.sample(100)
```

In [ ]:

```
import pandas as pd
import json

df_all=None
try:
    lines = open('./emotion/valid.txt',encoding='utf-8').readlines()
    # for line in lines:
    #      print(json.loads(line))
    lines = [json.loads(i) for i in lines]
    
    df_all = pd.DataFrame(lines)
except Exception as err:
    print(str(err))

# # # # df_all['label'].replace(0, '喜悦', inplace=True)
# df_all['label'] = df_all['情绪标签'].map({'happy':'幸福','sad':'悲伤','angry':'愤怒','neutral':'中性','surprise':'惊喜','fear':'恐惧'})
df_all['label'] = df_all['label'].map({'happiness':'幸福','sadness':'悲伤','disgust':'厌恶','anger':'愤怒','like':'喜欢','surprise':'惊喜','fear':'恐惧'})

# df_all.drop(labels='情绪标签',axis=1,inplace=True)
df_all.rename(columns = {'content':'text'}, inplace = True)
df_all.to_csv('valid.txt.ds.csv')
df_all.sample(100)
```

In [ ]:

```
df_all['label'].value_counts()
# df_all.columns
# df_all[['text', 'label']].to_csv('emotion_catalog_dataset.csv')
```



#### 合并多个pandas的dataframe数据

In [ ]:

```
import os
df_lst=[]
for f in os.listdir('./source/'):
    f = './source/'+f
    print(f)
    df = pd.read_csv(f)
    df_lst.append(df)
    print(df)
df_all = pd.concat(df_lst)
```

In [ ]:

```
#!/usr/bin/python

import sqlite3
table_name = 'simplifyweibo_4_moods'
conn = sqlite3.connect('sqlite_dt.db')
# 保存DataFrame数据到sqlite
df_all.to_sql(table_name,conn,schema=None,if_exists='replace',index=True,index_label=None, chunksize=None, dtype=None)
# # 读取sqlite表格数据到DataFrame
# df2=pd.read_sql_query("select * from {}".format(table_name), conn)
conn.close()
```

In [ ]:

```
df_all.columns
```

In [ ]:

```
#!/usr/bin/python

import sqlite3

conn = sqlite3.connect('sqlite_dt.db')
print ("数据库打开成功")
c = conn.cursor()
c.execute('''CREATE TABLE if not exists DataSet
       (id INTEGER PRIMARY KEY AUTOINCREMENT,
       Text             TEXT    NOT NULL,
       Label             CHAR(10) NOT NULL,
       Source            CHAR(50),
       Note              TEXT);''')
print ("数据表创建成功")
conn.commit()
conn.close()
```



### 将sqlite数据导入到pandas的DataFrame

In [ ]:

```
'''### 将sqlite数据导入到pandas的DataFrame'''
import pandas as pd
import sqlite3
conn = sqlite3.connect('sqlite_dt3.db')
# First pattern - turn query directly into dataframe:
df1 = pd.read_sql_query("SELECT * FROM DataSet", conn)
# Second pattern - get row-level data, but no column names
cur = conn.cursor()
results = cur.execute("SELECT * FROM DataSet LIMIT 5").fetchall()
df2 = pd.DataFrame(results)
conn.close()
```

In [ ]:

```
'''读取dedao的csv数据文件'''
import pandas as pd
dedao_df = pd.read_table('Dataset/DeDao_DS.csv',sep='\t')
```



#### 将dataframe数据按行插入到sqlite数据库

In [ ]:

```
'''将dataframe数据插入到sqlite数据库'''
#!/usr/bin/python
import sqlite3

conn = sqlite3.connect('sqlite_dt3.db')
c = conn.cursor()
print (">>>数据库打开成功")

# 创建数据表
c.execute('''CREATE TABLE if not exists DeDao_DS
       (id INTEGER PRIMARY KEY AUTOINCREMENT,
       Text             TEXT    NOT NULL,
       Label             CHAR(20) NOT NULL,
       Source            CHAR(50),
       Note              TEXT);''')
print (">>>数据表创建成功")
conn.commit()
# 插入数据
for index,row in dedao_df[:].iterrows():
    try:
        text=row['text'].replace('\'','')### 替换掉单引号,防止SQL插入失败
        label=row['label'].replace('\'','')
#     print("INSERT INTO DataSet (Text,Label) VALUES ({},{})".format(row['text'],row['label']))
        c.execute("INSERT INTO DeDao_DS (Text,Label) VALUES ('{}','{}')".format(text,label))
    except Exception as err:
        print(index,str(err))
conn.commit()
print (">>>数据插入成功")
conn.close()
```

In [ ]:

```
'''读取pickle文件的参数'''
import pickle
with open('catalog.pkl','rb') as fh:
   catalog_pkl = pickle.load(fh)
len(catalog_pkl),catalog_pkl
```



#### 生成paddlenlp数据集

In [ ]:

```
import pandas as pd
df_all=None
try:
    df_all = pd.read_csv('emotion_catalog_dataset.csv')
except Exception as err:
    print(str(err))
df_all=df_all[['text','label']]
df_all
```

In [ ]:

```
df_all['label'].value_counts()
```

In [ ]:

```
df_all['label'].replace('低落','悲伤',inplace=True)
```

In [ ]:

```
df_all[df_all['label'] == '厌恶']
```

In [ ]:

```
df_all.describe()
```

In [ ]:

```
df_all=df_all.dropna()
df_all=df_all.sample(frac=1).reset_index(drop=True)
```

In [ ]:

```
df_all.to_csv('emotion_catalog_dataset.csv',index=None)
```

In [ ]:

```
df_all['label'].value_counts().index.tolist()
labels = [i+'\n' for i in df_all['label'].value_counts().index.tolist()]
with open('label.txt','w',encoding='utf-8') as fhd:
    fhd.writelines(labels)
```

In [ ]:

```
df_all=df_all.sample(frac=1).reset_index(drop=True)
df_all[['text','label']][:100].to_csv('test.txt',sep='\t',header=None,index=None)
df_all[['text','label']][100:100000].to_csv('train.txt',sep='\t',header=None,index=None)
df_all[['text','label']][100000:110000].to_csv('dev.txt',sep='\t',header=None,index=None)
```

In [ ]:

```
df_all.sample(100)
```