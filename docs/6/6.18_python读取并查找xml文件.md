## python读取并查找xml文件

利用ElementTree，很容易就可以完成XML文档构建，并写入保存为文件。ElementTree对象的write方法就可以实现这个需求。

- ElementTree.parse(source, parser=None)，将xml文件加载并返回ElementTree对象。parser是一个可选的参数，如果为空，则默认使用标准的XMLParser解析器。

-  ElementTree.getroot()，得到根节点。返回根节点的element对象。
   Element.remove(tag)，删除root下名称为tag的子节点

-  以下函数，ElementTree和Element的对象都包含：

  ```
  find(match)，得到第一个匹配match的子节点，match可以是一个标签名称或者是路径。返回个element
  findtext(match,default=None)，得到第一个配置的match的element的内容
  findall(match)，得到匹配match下的所有的子节点，match可以是一个标签或者是路径，它会返回一个list，包含匹配的elements的信息
  iter(tag)，创建一个以当前节点为根节点的iterator
  ```

  

#### 读取xml文件代码如下：

```
from xml.etree.ElementTree import parse
# 开始分析xml文件
tree = parse('data.xml')
root = tree.getroot()
print(root.tag)
#查找title
title= root.findall('title')[0].text
print(f'title:{title}')
#查找speak_id
speak_id= root.findall('speak_id')[0].text
print(f'speak_id:{speak_id}')


# 查找XML文档中的元素
for child in root.findall('content'):
    # 获取元素的属性值或文本内容
    text = child.text
    print(f'text:{text}')
    #读取content的image属性
    image=child.attrib['image']
    print(image)
    print(f'attrib:{child.attrib}')

  
# for child in root:
#     print("tag:", child.tag)
#     print("text:", child.text)
#     print("attrib:", child.attrib)


# # 遍历XML文件中所有元素的值和标签名  
# for element in root.iter():  
#     # print(element.tag, element.text)
#     if element.tag=='title':
#         print(f'Title:{element.text}')
#     if element.tag=='content':
#         print(f'text:{element.text}')

#         print(f'attrib:{element.attrib}')
#         print(element.attrib['image'])

#     if element.tag=='speak_id':
#         print(f'speak_id:{element.text}')


```



#### xml文件：

```
<?xml version="1.0" encoding="utf-8"?>
<article name="xiaopch">
    <title>假如有一天我们可以将自己的意识上传</title>
    <speak_id>171</speak_id>
    <!-- 以下是章节 -->
    <content image = "1.png">
         如果有一天我们可以将自己的意识上传到电脑中，这将会是一个革命性的时刻，可能会对人类社会、科技和伦理道德产生深远的影响。以下是可能会发生的一些情况：
    </content>
    
    <content image = "2.png">
         1.人类意识的数字化和备份：一旦我们的意识可以上传到电脑中，那么我们就可以将其数字化，并备份到云端或其他存储介质中。这样，我们的意识就可以在不同的物理载体之间进行传输，或者在我们的身体死亡后得以永存。
         2.身体与意识的分离：上传意识后，我们的身体和意识将会被分离。这种情况下，我们可以通过更换身体来延长自己的寿命，也可以选择离开自己的身体，在虚拟世界中生活。
         3.新的社交和互动方式：上传意识后，我们可能会发现一种新的社交和互动方式。我们可以以数字化的形式与其他人交流，并在虚拟世界中建立新的社区。
         4.道德和伦理问题：上传意识可能会引发一系列道德和伦理问题。例如，如果我们的意识可以数字化并备份，那么是否意味着我们可以被复制？如果我们的意识可以离开身体，那么我们是否可以保持自己的身份和自我意识？这些问题将会引发激烈的讨论和争议。
         5.技术和法律的限制：尽管上传意识可能会带来很多好处，但技术和法律上是否能够实现，以及如何管理和监管意识上传技术，都将会是巨大的挑战。
    </content>
    <content image = "3.png">
         总之，如果我们的意识可以上传到电脑中，那么我们的生活将会发生巨大的改变。这是一个令人兴奋的未来，但也需要我们认真思考和应对其中的挑战。
    </content>

</article>

```

